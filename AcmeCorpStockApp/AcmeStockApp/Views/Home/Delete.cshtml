@model AcmeStockApp.Models.Product
@{
    ViewData["Title"] = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card">
                <div class="card-header">
                    <h3 class="card-title">Delete Product</h3>
                </div>
                <div class="card-body" style="height: 100%;">
                    <form asp-action="Delete">
                        <div class="input-group">
                            <input asp-for="Id" class="form-control" style="width: 100px;" />
                            <div class="input-group-append">
                                <button type="button" class="btn btn-secondary ml-3" onclick="clearFields()">Clear</button>
                            </div>
                        </div>
                        <div class="form-group">
                            <label asp-for="Name"></label>
                            <input asp-for="Name" class="form-control" style="width: 350px;" disabled />
                        </div>
                        <div class="form-group">
                            <label asp-for="Quantity"></label>
                            <input asp-for="Quantity" class="form-control" style="width: 200px;" disabled />
                        </div>
                        <div class="form-group">
                            <label asp-for="Price"></label>
                            <input asp-for="Price" class="form-control" style="width: 200px;" disabled />
                        </div>
                        <div class="form-group">
                            <label asp-for="Manufactured"></label>
                            <input asp-for="Manufactured" class="form-control" style="width: 210px;" disabled />
                        </div>

                        @if (!string.IsNullOrEmpty(ViewBag.ErrorMessage))
                        {
                            <div class="alert alert-danger" role="alert">
                                @ViewBag.ErrorMessage
                            </div>
                        }

                        <div class="card-footer" style="height: 100%; width: 100%;">
                            <div class="d-flex justify-content-end align-items-center h-100">
                                <button  type="submit" class="btn btn-primary ml-2">Find</button>
                                <button asp-controller="Home" asp-action="Index" id="del" onclick="clearFields()" type="submit" class="btn btn-warning ml-2">Delete</button>
                                <button asp-controller="Home" asp-action="Index" type="submit" class="btn btn-dark ml-2">Cancel</button>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        function clearFields() {

            document.getElementById("Id").value = "";
            document.getElementById("Name").value = "";
            document.getElementById("Quantity").value = "";
            document.getElementById("Price").value = "";
            document.getElementById("Manufactured").value = "";

            document.getElementById("del").value = "";

        }
    </script>
}
